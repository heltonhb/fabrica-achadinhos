# requirements.txt — Fábrica de Achadinhos
# Instalar: pip install -r requirements.txt

# Interface
streamlit>=1.30.0

# Google Sheets API (leitura/escrita)
google-api-python-client>=2.100.0
google-auth>=2.23.0
google-auth-oauthlib>=1.1.0

# Geração de roteiro (Gemini)
google-genai>=1.0.0

# Locução TTS — só para o render local legado (voz.py)
edge-tts>=6.1.0

# Processamento de áudio/trilha — só para o render local legado (trilha.py)
numpy>=1.24.0
scipy>=1.11.0

# Imagens (badge, CTA, legendas) — só para o render local legado (visual.py)
Pillow>=10.0.0

# Requisições HTTP
requests>=2.31.0

# Servidor Webhook (Bot Instagram)
fastapi>=0.109.0
uvicorn>=0.27.0
python-dotenv>=1.0.0

# Testes
pytest>=8.0.0
httpx>=0.27.0
